# Copy this file to `.env` and fill in your key. `.env` is gitignored.
#
# Get your key from renshuu: Tools -> Renshuu API.
# In CI this same value comes from the RENSHUU_API_KEY repository secret,
# so it never needs to be committed anywhere.
RENSHUU_API_KEY=
